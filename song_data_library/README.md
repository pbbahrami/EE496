# Song Data Library

Generated `song_data` header files are stored here.

Workflow in browser UI:
1. Pick MIDI from `midi_library/`
2. Build song_data entry (saved here)
3. Activate selected song_data (copies to `src/song_data.h`)
4. Build + upload firmware
