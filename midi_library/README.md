# MIDI Library Folder

Put your `.mid` / `.midi` files in this folder.

The browser companion can list files here and run:

- conversion to `src/song_data.h`
- optional firmware build/upload

This keeps song selection workflow in one place.
